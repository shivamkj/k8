apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nginx-ingress
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-cert"
    kubernetes.io/ingress.class: "nginx"
    argocd.argoproj.io/sync-wave: "2"
  labels:
    type: reverse-proxy
spec:
  tls:
    - secretName: cert-tls
      hosts:
        - k8.shivamjha.com
  rules:
    - host: k8.shivamjha.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: hello-k8-service
                port:
                  number: 8000
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nginx-ingress-1
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-cert"
    kubernetes.io/ingress.class: "nginx"
    argocd.argoproj.io/sync-wave: "2"
  labels:
    type: reverse-proxy
spec:
  tls:
    - secretName: cert-tls
      hosts:
        - k8.shivamjha.com
  rules:
    - host: k8.shivamjha.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: hello-k8-service-1
                port:
                  number: 8000